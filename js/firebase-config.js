/* ============================================================
   firebase-config.js
   Paste your Firebase web-app config below.
   Firebase Console -> Project settings -> Your apps -> SDK setup
   and configuration -> Config.

   This apiKey is PUBLIC by design — security is enforced by
   Firebase Authentication + Firestore security rules, not by
   hiding this key. It is safe to commit.

   Until real values are pasted, the app runs normally and the
   sign-in button shows "Sync off".
   ============================================================ */
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_PROJECT.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  appId: "PASTE_APP_ID"
};
